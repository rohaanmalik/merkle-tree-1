{
  "name": "merkle-tree-1",
  "version": "1.0.0",
  "description": "**Foundry is a blazing fast, portable and modular toolkit for Ethereum application development written in Rust.**",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "test": "forge test",
    "build": "forge build",
    "generate": "npx ts-node scripts/generate-merkle-tree.ts",
    "generate:verbose": "npx ts-node scripts/generate-merkle-tree.ts --verbose",
    "generate:test-data": "npx ts-node scripts/generate-test-data.ts",
    "verify": "npx ts-node scripts/verify.ts",
    "verify:all": "npx ts-node scripts/verify.ts",
    "verify-contract": "npx ts-node scripts/contract-verify.ts",
    "split": "npx ts-node scripts/split-distribution.ts",
    "split:upload": "npx ts-node scripts/split-distribution.ts --upload"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/storage": "^7.17.1",
    "@openzeppelin/merkle-tree": "^1.0.8",
    "commander": "^14.0.0",
    "csv-parser": "^3.2.0",
    "dotenv": "^17.2.2",
    "ethers": "^6.15.0",
    "typescript": "^5.9.2"
  }
}
